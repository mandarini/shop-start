// import * as admin from "firebase-admin";

// const db = admin.firestore();

export async function addProductIdToProductFunction(change: any, context: any) {
  /**
   * Here, we want to store the product's uid within the actual product document,
   * once it is written in our database.
   *
   * So, create a method that takes the product's Id from the context parameters,
   * and updates the newly written product object to contain it.
   *
   * Guard for errors!
   */
}
